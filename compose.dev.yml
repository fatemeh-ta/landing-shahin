services:
  landing-shahin:
    container_name: landing-front_cp
    build:
      context: .
      dockerfile: Dockerfile.dev

    ports:
      - "3000:3001"

    volumes:
      - .:/app
      - /app/node_modules

    environment:
      NODE_ENV: development

    restart: unless-stopped
